1.添加字段

默认放在最后

alter TABLE shopnc_member add `center_refuse_text` varchar (200) NOT NULL COMMENT '服务中心拒绝原因';

在 is_service_center 字段之后添加 center_refuse_text 字段

alter TABLE shopnc_member add `center_refuse_text`  varchar (200) NOT NULL COMMENT '服务中心拒绝原因' AFTER is_service_center;

alter TABLE shopnc_voucher modify voucher_store_id int(11) NOT NULL DEFAULT '0' COMMENT '可以使用此代金券的店铺id' AFTER voucher_limit ;


dfs168 后台-会员管理（需要给俊杰，在正式环境的数据库执行的sql语句）

alter table shopnc_member add `wechat_bind` enum('Y','N') NOT NULL DEFAULT 'N' COMMENT '微信绑定标识'; 


alter table shopnc_member modify `register_method` tinyint(2) NOT NULL DEFAULT '0' COMMENT '0为pc注册 1为app注册 2为wap注册 3微信注册';//修改


update shopnc_member set wechat_bind='Y' where member_id in (select member_id from shopnc_member_maping_wechat where member_id is not null);//更新数据加子查询


update shopnc_member set register_method =3 where member_passwd='' and register_method=0;//更新

alter TABLE shopnc_order add `center_id`  int(11) DEFAULT '0' COMMENT '服务中心id' AFTER rebate_id ;//添加字段
















